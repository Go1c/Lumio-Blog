{
  "name": "lumio-blog-deploy",
  "private": true,
  "packageManager": "pnpm@9.7.0",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "pnpm -C code install --frozen-lockfile --prod=false && pnpm -C code build",
    "start": "pnpm -C code start",
    "typecheck": "pnpm -C code typecheck"
  }
}
